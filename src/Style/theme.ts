import { createTheme } from '@material-ui/core/styles';

export const GreenTheme = createTheme({
  palette: {
    primary: {
      main: '#009688'
    },
    secondary: {
      main: '#ff3d00'
    }
  }
});
