import { createMuiTheme } from '@material-ui/core/styles';

export const GreenTheme = createMuiTheme({
  palette: {
    primary: {
      main: '#009688'
    },
    secondary: {
      main: '#ff3d00'
    }
  }
});
